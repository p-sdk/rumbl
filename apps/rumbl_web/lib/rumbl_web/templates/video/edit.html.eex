<h1><%= gettext "Edit Video" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.video_path(@conn, :update, @video)) %>

<span><%= link gettext("Back"), to: Routes.video_path(@conn, :index) %></span>
